diff --git a/packages/drawnix/src/hooks/useTaskStorage.ts b/packages/drawnix/src/hooks/useTaskStorage.ts
--- a/packages/drawnix/src/hooks/useTaskStorage.ts
+++ b/packages/drawnix/src/hooks/useTaskStorage.ts
@@ -21,0 +22 @@ import {
+  imageGenerationRecoveryService,
@@ -111 +112,6 @@ export function useTaskStorage(): boolean {
-            if (!isImageRequestRecoveryCandidate(recoveryTask)) {
+            if (
+              !isImageRequestRecoveryCandidate(recoveryTask) ||
+              !imageGenerationRecoveryService.supportsRecoveryProtocol(
+                recoveryTask
+              )
+            ) {
@@ -179 +185,4 @@ export function useTaskStorage(): boolean {
-                  isImageRequestRecoveryCandidate(imageRecoveryTask)
+                  isImageRequestRecoveryCandidate(imageRecoveryTask) &&
+                  imageGenerationRecoveryService.supportsRecoveryProtocol(
+                    imageRecoveryTask
+                  )
@@ -214,0 +224,10 @@ export function useTaskStorage(): boolean {
+                if (
+                  task.type === TaskType.IMAGE &&
+                  imageRecoveryTask &&
+                  isImageRequestRecoveryCandidate(imageRecoveryTask)
+                ) {
+                  errorMessage =
+                    '图片请求已提交，但无法确认其结果；未自动重试以避免重复生成';
+                  errorCode = 'IMAGE_RESULT_UNCONFIRMED';
+                }
+
diff --git a/packages/drawnix/src/services/image-generation-recovery-service.ts b/packages/drawnix/src/services/image-generation-recovery-service.ts
--- a/packages/drawnix/src/services/image-generation-recovery-service.ts
+++ b/packages/drawnix/src/services/image-generation-recovery-service.ts
@@ -558,0 +559,7 @@ export class ImageGenerationRecoveryService {
+  supportsRecoveryProtocol(task: ImageGenerationRecoveryTask): boolean {
+    const descriptor = this.createTaskDescriptor(task);
+    return Boolean(
+      descriptor && this.resolveTrustedInvocationRoute(descriptor)
+    );
+  }
+
@@ -692,0 +700,15 @@ export class ImageGenerationRecoveryService {
+  ): InvocationPlan | null {
+    const plan = this.resolveTrustedInvocationRoute(task);
+    if (
+      !plan ||
+      !plan.provider.apiKey?.trim() ||
+      CryptoUtils.isEncrypted(plan.provider.apiKey.trim())
+    ) {
+      return null;
+    }
+
+    return plan;
+  }
+
+  private resolveTrustedInvocationRoute(
+    task: RecoveryTaskDescriptor
@@ -719,2 +740,0 @@ export class ImageGenerationRecoveryService {
-      !plan.provider.apiKey?.trim() ||
-      CryptoUtils.isEncrypted(plan.provider.apiKey.trim()) ||
