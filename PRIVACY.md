# 隐私说明

U-King 不为你的对话、文件或凭据运营任何后端服务。
（可选的官方中转服务与官网服务由「火火的(深圳)科技有限公司」运营，详见官网隐私政策。）
除下面「会发出的」一节逐条列明的内容外，数据只存在你的电脑上。

## 永远不出本机的

- API key / 凭据（存于 `~/.claude`、`~/.codex` 等本地文件，不加密，不上传）
- 与 AI 工具的对话内容
- 任务 / 会话记录
- 模型供应商配置

## 会发出的

- **模型调用**：请求直接发往你在供应商配置里指定的端点
- **可选的官方中转服务（虾盘云）**：如果你选择充值使用，设备会生成一个设备 Key（基于机器指纹）用于计费与余额查询；不用该服务则不产生任何相关请求（可以是官方 API、第三方中转，也可以是虾盘云中转服务），走的是你自己的网络路径，不经过 U-King 服务器中转
- **Bug 上报**（默认仅在安装失败 / 程序崩溃时触发，逐项列明）：
  - 错误堆栈与日志（路径已脱敏，用户名替换为占位符）
  - 操作系统版本、U-King 版本
  - 触发上报的动作 ID（如"装 Node 失败"），不含具体参数值
  - 截图：**不会自动上报**，仅在用户手动点击"附上截图"后才会包含，且发送前可预览

## 远程协助（默认关闭）

- 默认**不开启**，不开启则不下载任何组件、不发起任何连接
- 需要你在「意见反馈」页主动开启后，才会下载官方 agent 并连接中转服务器
- 开启期间维护者可在你的电脑上执行命令以排查故障；可随时点「停止协助」中断，2 小时自动断开
- 每条执行过的命令都记入本机审计日志（`%TEMP%\uclaw\sessions\<日期>.log`），界面上可直接打开查看

## 绝不上报的

- API key / 任何凭据
- 对话内容、任务内容
- 用户文件内容
- 未经用户手动确认的截图

## 脱敏机制

Bug 上报生成前会对日志做以下处理：

- 文件路径中的用户名替换为固定占位符
- 已知的 key / token 格式字段做正则过滤剔除
- 截图默认不采集，采集需用户在弹窗中主动确认

自动上报目前没有应用内开关；如果你希望增加开关，欢迎开 issue 讨论。
