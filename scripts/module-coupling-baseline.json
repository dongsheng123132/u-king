{
  "pairs": 47,
  "why": "P2 引入 usb_targets 抽象层（合流方案 §2 / §3.3）。usb_genie 仍是 P1 真机验收过的 portable AI 运行时实现，delegated_target_* / action_target_list 都直接调它以保零行为变化；P3 接入 U-Claw 时（U-Claw manifest 用 http-api transport 不走 usb_genie）自然解耦。",
  "list": [
    "agent → device",
    "agent → journal",
    "agent → origin",
    "agent → providers",
    "agent → video",
    "bundled_apps → miniapp",
    "claude_proxy → codex_proxy",
    "clawx → tools",
    "cleanup → codex_proxy",
    "cleanup → context_menu",
    "cleanup → identity",
    "cleanup → mcp",
    "cleanup → providers",
    "cleanup → skillpack",
    "cleanup → toolbox",
    "cleanup → tools",
    "cleanup → uninstall",
    "cleanup → uuswitch",
    "codex_proxy → device",
    "codex_proxy → providers",
    "device → providers",
    "expert → skillpack",
    "feedback → codex_proxy",
    "feedback → device",
    "feedback → envfp",
    "feedback → metrics",
    "feedback → providers",
    "feedback → report",
    "guard → device",
    "install → guard",
    "miniapp → artifacts",
    "miniapp → image",
    "providers → tools",
    "providers → usage",
    "report → crashlog",
    "report → device",
    "report → feedback",
    "report → metrics",
    "toolprobe → tools",
    "tools → install",
    "tools → providers",
    "tools → term",
    "tools → toolprobe",
    "tools → uuswitch",
    "usb_targets → usb_genie",
    "uuswitch → device",
    "uuswitch → providers"
  ]
}
